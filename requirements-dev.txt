# Test/development dependencies only. Install with:
#   pip install -r requirements-dev.txt
# The production Docker image installs requirements.txt (no test deps).
-r requirements.txt
pytest>=8.2
respx>=0.23.1
pillow>=12.3.0
